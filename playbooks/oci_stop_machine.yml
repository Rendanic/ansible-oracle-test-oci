---

- name: Stop machines from hostgroup
  hosts: "oci_devsec"
  any_errors_fatal: true
  tasks:
    - name: OCI Executions
      delegate_to: localhost
      block:
        - name: show id
          debug:
            var: id
